description_declare_variables = """Declare the function is_tidy which takes number as a parameter and should output True
if the numbers are in non-descending order, or False if not.
Note: Input values ​​are always positive.
Examples:
    
    is_tidy (12) == True The numbers {1, 2} are in non-descending order, ie 1 <= 2.
    
    is_tidy (32) == False The numbers {3, 2} are in descending order, ie 3> 2.
    
    is_tidy (1024) == False The numbers {1, 0, 2, 4} are in descending order because 0 <1.
    
    is_tidy (3445) == True The numbers {3, 4, 4, 5} are in descending order, because 4 <= 4.
    
    is_tidy (13579) == True The numbers {1, 3, 5, 7, 9} are in ascending order."""