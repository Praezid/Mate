description_declare_variables = """Write a split_string function that takes a string and returns a list of strings of two characters.
If the string contains an odd number of characters, then the second character must be replaced with an underscore ("_").
Example:
    split_string('abc') == ['ab', 'c_']
    split_string('abcdef') == ['ab', 'cd', 'ef']"""