description_declare_variables = """Create a check_number function that checks a number for three different properties:
    -Is this a positive number?
    -Is this an even number?
    -Is this a multiple of 10?
The check_number function should return a list of test results as Boolean values.
The number must always be an integer.
Example:
    check_number(3) == [True, False, False]
    check_number(10) == [True, True, True]
    check_number(0) == [False, True, True]
    check_number(-1) == [False, False, False]"""