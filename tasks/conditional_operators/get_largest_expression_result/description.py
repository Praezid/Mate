description_declare_variables = """Create a function get_largest_expression_result, which takes two numbers a and b and returns the largest possible result among the expressions:
    a + b
    a - b
    a * b
    a / b
Notes:
    Keep in mind that a and b can be negative.
    Do not use the ternary operator or the keyword else.
    Do not use the math module.
Hint:
    If the function returns None, the code may not take into account the fact that the maximum values of the expressions can be equal (for example, 2 + 2 = 2 * 2)."""