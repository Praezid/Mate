description_declare_variables = """Declare a new variable result_string with the text `Hello, world!`,
using line interpolation and already declared variables `a` and `b`.
Note: Pay attention to the punctuation marks that should be present as a result."""
